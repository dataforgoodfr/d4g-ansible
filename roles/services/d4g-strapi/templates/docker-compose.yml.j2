services:
  app:
    image: ghcr.io/dataforgoodfr/dataforgoodfr.github.io-backend:latest
    env_file: './strapi.env'
    networks:
      - d4g-internal
    deploy:
      replicas: {{ replicas }}
      placement:
        constraints:
          - node.hostname == {{ deploy_on }}

networks:
  d4g-internal:
    external: true
