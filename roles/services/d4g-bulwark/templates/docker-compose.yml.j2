services:
  app:
    image: ghcr.io/bulwarkmail/webmail:latest
    env_file: ./bulwark.env
    networks:
      - d4g-internal
    deploy:
      replicas: {{ replicas }}
      placement:
        constraints:
          - node.hostname == {{ deploy_on }}

networks:
  d4g-internal:
    external: true
